#[derive(Debug)]
pub enum Cmods {
    Zabuton,
    Kiff,
    Fillet,
    Typhon,
}

#[derive(Debug)]
pub enum Umods {
    Krishna,
    Orion,
    Pegasus,
    Echidna,
    Suki,
    Monad,
}

#[derive(Debug)]
pub enum Omods{
    Stati,
    Stratus,
    Aether,
    Liubyt,
}
