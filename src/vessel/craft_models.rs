#[derive(Debug)]
pub enum Cmods {
    Zabuton,
    Kil,
    Fillet,
    Typhon,
}

#[derive(Debug)]
pub enum Umods {
    Krishna,
    Orion,
    Pegasus,
    Echidna
}

#[derive(Debug)]
pub enum Omods{
    Stati,
    Stratus,
    Monad,
    Aether,
}
